# macOS metadata
.DS_Store

# Node.js dependencies (regenerated via `npm install`)
node_modules/

# 11ty build output (regenerated via `npm run build`)
_site/

# npm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
